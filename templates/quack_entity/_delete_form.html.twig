<form method="post" action="{{ path('quack_entity_delete', {'id': quack_entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quack_entity.id) }}">
    <button class="btn">Delete</button>
</form>
